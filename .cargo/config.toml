[alias]
# 開発用ホットリロード: ソース変更を監視して自動で再ビルド＆再起動する。
# 事前に `cargo install cargo-watch` が必要。
# `shoki-core` も監視する（#187）。落とすと、core を直しても再ビルドが走らない。
dev = "watch -w src -w ui -w build.rs -w Cargo.toml -w shoki-core -x run"
